resources:
  pipelines:
    pipeline_patient_analytics:
      name: patient_analytics
      clusters:
        - label: default
          node_type_id: Standard_D4s_v3
          num_workers: 1
      libraries:
        - glob:
            include: ../src/patient_analytics/transformations/**
      schema: bronze
      catalog: dev
      root_path: ../src/patient_analytics
